import 'package:flutter/widgets.dart';

class RepairWidget extends StatelessWidget {
  final String type;
  RepairWidget({this.type = 'ys'});

  @override
  Widget build(BuildContext context) {
    return Text('dmeo');
  }
}
